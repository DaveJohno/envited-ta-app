import Create from "../create/Componants/create";

export default function Event(eventData) {
  console.log(eventData);
  return (
    <main className="eventContainer">
      <h1>hello world</h1>
    </main>
  );
}
